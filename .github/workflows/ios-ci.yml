name: iOS CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    paths:
      - 'LittleMoments/**'
      - 'Tuist/**'
      - 'fastlane/**'
      - 'Gemfile*'
      - 'Project.swift'
      - '**/*.swift'
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: macos-15
    timeout-minutes: 60

    env:
      SCHEME: LittleMoments
      UI_SCHEME: LittleMoments-UI
      DEVICE: "iPhone 17 Pro"
      SCAN_DESTINATIONS: "platform=iOS Simulator,name=iPhone 17 Pro,OS=latest"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print Xcode version
        run: xcodebuild -version

      - name: List available simulators
        run: xcrun simctl list devices

      - name: Set up Ruby with Bundler cache
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2.2'
          bundler-cache: true

      - name: Install build tooling
        run: |
          brew update
          brew install tuist xcbeautify

      - name: Generate Xcode project
        run: bundle exec fastlane generate

      - name: Fastlane version
        run: bundle exec fastlane --version

      - name: Build & test via Fastlane (scan)
        run: |
          set -e
          bundle exec fastlane ci

      # - name: Build & test via xcodebuild (alternative)
      #   if: failure()
      #   run: |
      #     set -e
      #     mkdir -p build
      #     xcodebuild \
      #       -scheme "$SCHEME" \
      #       -destination "platform=iOS Simulator,name=iPhone 17 Pro,OS=latest" \
      #       -resultBundlePath build/Result.xcresult \
      #       clean build test | xcbeautify

      - name: Upload build results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xcresult-or-logs
          path: |
            build/UnitTests.xcresult
            build/UITests.xcresult
            ~/Library/Logs/scan
          if-no-files-found: ignore
          retention-days: 7
